**IF EVERYTHING (images) BROKEN**

Close the project and open the folder that holds the project folder
All the source links include /BloomingtonIN because that is what the url is when live
So when we open it, we need to open one folder previous and select BloomingtonIN
That way the url is the same
So open the CascadeStorymap Folder that contains BloomingtonIN